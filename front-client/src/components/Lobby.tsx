const Lobby = () => {
    return (
        <div className="h-full flex items-center justify-center text-center">
            <span className="text-lg tracking-tighter">Relaxe, o jogo já vai começar!</span>
        </div>
    );
};

export default Lobby;
